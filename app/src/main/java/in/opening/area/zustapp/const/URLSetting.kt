package `in`.opening.area.zustapp.const

import javax.inject.Inject
import javax.inject.Singleton

@Singleton
class URLSetting @Inject constructor() {
    fun getUrl():String {
         return ""
    }
}